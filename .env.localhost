# =============================================================================
# Local machine — vite --mode localhost / npm run dev:local
# Override with gitignored .env.localhost.local if needed
# =============================================================================
VITE_APP_ENV=dev
VITE_BACKEND_API_URL=http://localhost:8080
VITE_API_VERSION_PATH=/api/v1.0.0
